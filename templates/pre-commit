#!/bin/bash
# Pre-commit hook for Mauscribe
# Runs before git commit to perform linting and code quality checks

echo "🔍 Pre-commit hook triggered - Running code quality checks..."

# Get the current directory (repository root)
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

# Check if there are staged changes
if git diff --cached --quiet; then
    echo "⚠️  No staged changes to commit"
    echo "💡 Use 'git add' to stage files before committing"
    exit 1
fi

# Check if .venv exists and activate it
if [ -d ".venv" ]; then
    echo "🐍 Activating virtual environment (.venv)..."

    # Activate virtual environment
    if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "cygwin" || "$OSTYPE" == "win32" ]]; then
        # Windows
        source .venv/Scripts/activate
    else
        # Unix/Linux
        source .venv/bin/activate
    fi

    echo "✅ Virtual environment activated"
    echo ""
else
    echo "⚠️  Virtual environment (.venv) not found"
    echo "💡 Create virtual environment first: python -m venv .venv"
    echo ""
fi

# Run pre-commit checks if available
if [ -f ".pre-commit-config.yaml" ]; then
    echo "🔍 Running pre-commit checks..."
    echo "=================================================="

    # Check if pre-commit is installed, install if not
    if ! command -v pre-commit &> /dev/null; then
        echo "📦 Pre-commit not installed, installing now..."

        # Try to install pre-commit
        if [ -d ".venv" ]; then
            # Use pip from virtual environment
            if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "cygwin" || "$OSTYPE" == "win32" ]]; then
                # Windows
                .venv/Scripts/pip install pre-commit
            else
                # Unix/Linux
                .venv/bin/pip install pre-commit
            fi
        else
            # Fallback to system pip
            python -m pip install pre-commit
        fi

        if [ $? -eq 0 ]; then
            echo "✅ Pre-commit installed successfully"

            # Install pre-commit hooks
            echo "🔧 Installing pre-commit hooks..."
            if [ -d ".venv" ]; then
                if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "cygwin" || "$OSTYPE" == "win32" ]]; then
                    .venv/Scripts/pre-commit install
                else
                    .venv/bin/pre-commit install
                fi
            else
                pre-commit install
            fi
            echo "✅ Pre-commit hooks installed"
        else
            echo "❌ Failed to install pre-commit"
            echo "💡 Will run basic checks instead"
        fi
    fi

    # Now try to run pre-commit
    if command -v pre-commit &> /dev/null; then
        # Run pre-commit on staged files
        echo "🚀 Running pre-commit checks..."
        pre-commit run

        if [ $? -ne 0 ]; then
            echo ""
            echo "❌ Pre-commit checks failed! Commit aborted."
            echo "💡 Fix the linting issues before committing"
            echo "💡 Run 'pre-commit run --all-files' to check all files"
            exit 1
        else
            echo ""
            echo "✅ All pre-commit checks passed!"
            echo "=================================================="
        fi
    else
        echo "⚠️  Pre-commit still not available, running basic checks instead..."
        echo ""

        # Run basic checks manually
        echo "🔍 Running basic code quality checks..."

        # Check Python files with basic syntax check
        PYTHON_FILES=$(git diff --cached --name-only | grep '\.py$')
        if [ -n "$PYTHON_FILES" ]; then
            echo "🐍 Checking Python files for syntax errors..."
            for file in $PYTHON_FILES; do
                if [ -f "$file" ]; then
                    python -m py_compile "$file"
                    if [ $? -eq 0 ]; then
                        echo "✅ $file - Syntax OK"
                    else
                        echo "❌ $file - Syntax error!"
                        echo "💡 Fix syntax errors before committing"
                        exit 1
                    fi
                fi
            done
        fi

        # Check for trailing whitespace
        TRAILING_WHITESPACE=$(git diff --cached | grep -E "^\+.*[[:space:]]+$" | wc -l)
        if [ $TRAILING_WHITESPACE -gt 0 ]; then
            echo "⚠️  Warning: Found $TRAILING_WHITESPACE lines with trailing whitespace"
            echo "💡 Consider removing trailing whitespace"
        fi

        echo "✅ Basic checks completed"
        echo "=================================================="
    fi
else
    echo "⚠️  Pre-commit config not found: .pre-commit-config.yaml"
    echo "💡 No linting checks will be performed"
    echo ""
fi

# Check for common issues in staged files
echo "🔍 Checking staged files for common issues..."

# Check for large files
LARGE_FILES=$(git diff --cached --name-only | xargs ls -la 2>/dev/null | awk '$5 > 1048576 {print $9}' | head -5)
if [ -n "$LARGE_FILES" ]; then
    echo "⚠️  Warning: Large files detected (>1MB):"
    echo "$LARGE_FILES"
    echo "💡 Consider if these files should be committed"
    echo ""
fi

# Check for merge conflict markers
CONFLICT_MARKERS=$(git diff --cached | grep -E "^[+-]<<<<<<<|^[+-]=======|^[+-]>>>>>>>" | wc -l)
if [ $CONFLICT_MARKERS -gt 0 ]; then
    echo "❌ Merge conflict markers detected! Commit aborted."
    echo "💡 Resolve merge conflicts before committing"
    exit 1
fi

# Check for TODO/FIXME comments in staged files
TODO_COMMENTS=$(git diff --cached | grep -i "TODO\|FIXME\|HACK\|XXX" | wc -l)
if [ $TODO_COMMENTS -gt 0 ]; then
    echo "⚠️  Warning: Found $TODO_COMMENTS TODO/FIXME comments in staged files"
    echo "💡 Consider addressing these before committing"
    echo ""
fi

echo "✅ Pre-commit checks completed successfully"
echo "�� Ready to commit!"
